목표
** 외부의 좌표계 이동명령을 받아 Isaac SIM에 전달하여 로봇을 시뮬레이션 할 것 **


역할 별 구조
1. Isaac SIM과 통신하는 노드 (액션서버 + 상태 모니터링 토픽)
2. 외부에서 좌표 이동 명령을 받아 로봇좌표로 변환하고 Isaac SIM에게 전달하는 액션 클라이언트 노드
3. 외부 좌표계를 받아 로봇에게 이동명령을 하달하는 노드 - "Command 노드"


메인 시나리오
1. 상부 카메라에서 객체를 탐지하고, 해당 객체의 위치로 이동하는 명령을 액션 클라이언트에 전달.
2. 액션 클라이언트는 전달받은 명령을 순서대로 수행. 액션 서버로 이동명령하고, 이동상태나 완료여부 확인 등 로봇 제어상태를 확인한다.
3. 액션 서버는 액션 클라이언트로부터 전달받은 명령을 시뮬레이션 환경에 전달하고, 그 상태를 확인한다.


** 제한사항
- 상부 카메라의 객체탐지 상황을 가정하고 임의의 YOLO 결과데이터를 받는다. 결과 데이터는 객체중심 좌표와 bbox, 클래스, 점수(0.85이상)로 구성.

- 픽셀좌표를 로봇좌표로 매핑도 되어 있다고 가정한다. 변환하는 과정은 코드에 기술하되 추후 실물 카메라나 시뮬레이션 카메라로부터 매핑하는 과정이 추가될 예정이다.
