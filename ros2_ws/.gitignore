# ==========================================
# ros2_ws 전용 규칙
# 공통 규칙(OS/IDE, Python 캐시·venv·패키징, 모델 파일)은 루트 .gitignore에 정의
# ==========================================

# ------------------------------------------
# ROS2 / Colcon 빌드 산출물
# ------------------------------------------
build/
install/
log/
.ament_index/

# ------------------------------------------
# 모델 가중치 (학습 / 추론 / 변환 산출물)
# ------------------------------------------
*.pt
*.pth
*.onnx

# ------------------------------------------
# 로컬 데이터셋 및 MCAP / Rosbag 녹화물
# ------------------------------------------
*.mcap
*.db3
*.bag
data/
datasets/
recordings/

# ------------------------------------------
# 캘리브레이션 행렬
# ------------------------------------------
*.npy

# ------------------------------------------
# 에디터 임시 파일 (vim swap)
# ------------------------------------------
*.sw?

# ------------------------------------------
# 외부 로봇 드라이버 (upstream clone)
# ------------------------------------------
src/feetech_ros2_driver/
